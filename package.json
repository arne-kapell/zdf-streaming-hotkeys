{
  "name": "zdf-streaming-hotkeys",
  "version": "1.0.0",
  "description": "Chrome-Extension: YouTube-ähnliche Hotkeys für die ZDF Mediathek",
  "private": true,
  "scripts": {
    "test": "node test/test-live.js",
    "build": "zip -r \"zdf-hotkeys-$(node -p 'require(\"./package.json\").version').zip\" manifest.json content.js background.js overlay.css popup.html popup.css popup.js icons/"
  },
  "devDependencies": {
    "puppeteer-core": "^22.0.0"
  }
}
